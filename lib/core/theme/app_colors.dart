import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
static const Color secondary = Color(0xFF7C4DFF);
  // Background
  static const Color backgroundStart = Color(0xFF0F172A);
  static const Color backgroundMiddle = Color(0xFF1D4ED8);
  static const Color backgroundEnd = Color(0xFF06B6D4);

  // Primary
  static const Color primary = Color(0xFF2563EB);
  static const Color accent = Color(0xFF06B6D4);

  // Glass
  static const Color glass = Color.fromRGBO(255, 255, 255, 0.12);
  static const Color glassBorder = Color.fromRGBO(255, 255, 255, 0.20);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Buttons
  static const Color buttonStart = Color(0xFF06B6D4);
  static const Color buttonEnd = Color(0xFF2563EB);

  // Error
  static const Color error = Colors.redAccent;
}